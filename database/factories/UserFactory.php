<?php

namespace Database\Factories;

use Illuminate\Database\Eloquent\Factories\Factory;
use Illuminate\Support\Str;

class UserFactory extends Factory
{
    /**
     * Define the model's default state.
     *
     * @return array
     */
    public function definition()
    {
        return [
            'USE_NOMBRE' => $this->faker->name(),
            'USE_EMAIL' => $this->faker->unique()->safeEmail(),
            'USE_EMAIL_VERIFIED_AT' => now(),
            'USE_PASSWORD' => '$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi', // password
            'USE_REMEMBER_TOKEN' => Str::random(10),
        ];
    }

    /**
     * Indicate that the model's email address should be unverified.
     *
     * @return \Illuminate\Database\Eloquent\Factories\Factory
     */
    public function unverified()
    {
        return $this->state(function (array $attributes) {
            return [
                'USE_EMAIL_VERIFIED_AT' => null,
            ];
        });
    }
}
